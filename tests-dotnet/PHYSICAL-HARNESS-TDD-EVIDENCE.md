# DAWN PRO2 Physical Harness TDD Evidence

The 2026-08-09 offline-only process-lineage diagnostic and real published-apphost topology regression is documented separately in `tests-dotnet/OFFLINE-PHYSICAL-LINEAGE-TDD-EVIDENCE.md`. It preserves strict direct-parent authorization based on the experimentally observed self-contained apphost topology and leaves runtime approval fail-closed.

Date: 2026-08-08

No physical category, watchdog execute/recovery supervision, PREPARE, EXECUTE, RECOVERY, HID access, physical environment variable assignment, approval population, hardware session, hardware result publication, device write, flash operation, commit, or push was used during the fifteenth remediation's coding or verification. Its factual basis remains the user's earlier actual read-only PREPARE work: the pinned DAWN PRO2 PID `0x011D`, firmware raw string exactly `1.5`, twice completed full consistent raw preflight with raw Active EQ `9`. That observation is not proof of a physical default/custom toggle and is distinct from PEQ registry profile `7`. This remediation used fake transports and software-only build/fingerprint/smoke modes only.

## RED to GREEN slices

| Slice | RED evidence | GREEN evidence |
|---|---|---|
| Retained-stream cancellation | `RetainedHidStreamDrainsCanceledReadBeforeNextRequest` failed: expected next firmware `2.0`, actual stale firmware `1.5`. | The integrated real `HidSharpDawnPro2Transport` plus `DawnPro2Device` test passed; the three cancellation-focused tests passed 3/3. |
| Opaque response bytes | Compilation failed with `CS1061`: `RawPeqBandState` had no `OpaqueByte4` or `OpaqueByte35`. | `Pro2RawReadKeepsUnprovenBandAndPresetBytesOpaque` passed 1/1. |
| Persisted IndividualBand | `SessionValidationRejectsMutatedPersistedIndividualBand` failed: expected `InvalidDataException`, but no exception was thrown. | The same focused test passed 1/1 after exact index plus 63-byte comparison was added. |
| Recovery compatibility | Compilation failed with `CS0103`: `PhysicalRecoveryCompatibility` did not exist. | Stale-state, reachable-partial-state, and firmware-drift tests passed 3/3. |
| Recovery resumability | Compilation failed with missing `PhysicalRecoveryStep` and `PhysicalRecoveryResumePlan` types. | The seven required durable-phase rows passed 7/7. |
| Real execute orchestration | Compilation failed with missing `PhysicalExecuteStep`, `IPhysicalExecuteActions`, and `PhysicalExecuteOrchestrator`. | Managed failure injection passed at all 12 action stages; the combined execute-orchestrator focus later passed 23/23. |
| Session fallback publication | Compilation failed with `CS0117`: `PhysicalSessionStore` had no `RecoveryCopyPath`. | Missing-primary fallback discovery passed 1/1. |
| Default-safe settings | Compilation failed with `CS0103`: `PhysicalRunSettingsGate` did not exist. | The inherited-opt-in-as-data/default-runsettings proof passed 1/1. A default unfiltered run later passed 169 selected software tests with 0 skipped; three physical categories were excluded. |
| External watchdog | Compilation failed because namespace `Moondrop.PhysicalWatchdog` and its policy types did not exist. | Command, durable-decision, ownership, phase-contract, inactivity, and dry-run tests passed in the final focused safety set. |
| Raw execute/recovery rejection | Compilation failed with `CS0103`: `PhysicalWatchdogProcessGate` did not exist. | Runner-token and canonical-heartbeat authorization tests passed. |
| Redacted dry run | Compilation failed with `CS0117`: `PhysicalTestCommandBuilder.DescribeForDryRun` did not exist. | The same focused test passed 1/1 and proves the prepared confirmation is replaced with `[REDACTED]`. |
| Truthful final restoration publication | The `RestorationVerified` injection row failed: expected last durable phase `RestorationVerified`, actual `AwaitingRestorationPhysicalCycle`. | All five immediate-restoration publication-failure rows passed 5/5 after final-phase republish was added. |
| Canonical raw restoration | Hostile captured bytes 4/35 were replayed and three exact protocol/device tests failed. | The same 3/3 tests prove canonical band/slot selectors, zeroed reserved bytes, and copying only proven bytes 7-33. |
| Transaction isolation and poisoning | Concurrent callers issued two writes while the first response was still owed; late-timeout reuse was accepted. | Concurrency, cancellation drainage, timeout poisoning, malformed-envelope disposal, and cancellation-before-write tests pass. |
| Phase-safe recovery | `RestorationWritesVerified`, `RestorationFlashSaveStarting`, and `AwaitingRestorationPhysicalCycle` skipped RAM restore. | All seven phase rows plus the power-cycle-at-flash-start real orchestrator regression pass; original RAM restoration precedes flash. |
| Durable lineage | Divergent valid primary/recovery publications selected the newer timestamp. | Test and watchdog readers isolate malformed copies and reject divergent valid lineage. |
| Parent authentication | A correctly shaped token/heartbeat path was accepted without a watchdog parent. | Injectable process-identity coverage proves an actual direct canonical watchdog parent succeeds and a forged raw-dotnet parent fails. |
| Execute truth/recovery retries | Successful recovery could return overall exit `0`; recovery launched only once. | Execute failure stays nonzero with `EXECUTE FAILED; RECOVERY VERIFIED`; retry policy is bounded to three recoverable attempts. |
| Liveness and source proof | A 24-transaction watchdog-protected snapshot could exceed the 15-second idle bound; prebuilt binaries lacked freshness proof. | Execute/recovery double snapshots emit exactly 24 authenticated transaction heartbeats, while direct read-only PREPARE completes the same 24 bounded reads without a watchdog; SHA-256 drift and mandatory fresh-build policies are covered. |
| Direct read-only PREPARE progress | The actual read-only PREPARE parsed its first firmware response, then failed because the shared transaction callback demanded an authenticated watchdog parent. The new focused test first failed to compile because the phase-specific opener/progress/planner interfaces did not exist. | PREPARE alone now uses a private no-op transaction-progress capability and its fake transport completes 24 read-only packets through consistent snapshot and Prepared session planning without watchdog variables. Execute/recovery use a separate opener that reauthenticates before transport open and retains authenticated transaction pulses; missing/invalid authorization passed 2/2 phase rows with zero opens or packets. |
| Ambiguous request write | A fake queued the late response and then threw a write timeout; the next request consumed it because the channel stayed reusable. | The focused write/cancellation set proves disposal, zero reads, one unconsumed late response, and rejection of the next call. |
| Semantic response poison | Exact 64-byte invalid raw-band/gain payloads drained fully but left the retained channel reusable. | Firmware, active-EQ, gains, ordinary/raw band parsing now occurs inside the poison boundary; representative malformed band/gain tests prove no reuse. |
| Watchdog zero invariant | Child exit 0 while durable phase remained Prepared returned 0, and replacement lineage could be reported as verified. | Policy and executable integration require freshly loaded matching-lineage Completed; recovery after execute failure remains nonzero. |
| Post-load ownership | Parent heartbeat authenticated session A before the child loaded a replacement session B. | The child compares loaded ID/token/source/runtime/canonical Original/Plan lineage with authenticated heartbeat values before HID open. |
| Independent approval | PREPARE trusted and captured whatever source/binaries existed at runtime. | Strict excluded schema-v1 metadata must match both deterministic source and deterministic complete self-contained runtime hashes plus every declared source/runner/watchdog/metadata count; both placeholders keep PREPARE NO-GO. |
| Complete fresh runtime | Dependency DLLs, deps/runtimeconfig files, and restore inputs were outside the fingerprint; only the watchdog DLL was self-checked. | Locked restores and isolated publishes feed a complete path-independent runtime manifest, full watchdog directory check, and child-side exact recomputation. |
| Self-contained runner startup | The exact published physical apphost crashed before discovery with `FileNotFoundException: Microsoft.CodeCoverage.Core`. | The physical locked graph now uses explicit MSTest adapter/framework plus TRX packages without the unused coverage extension; direct `--help` exits `0` with shared-runtime paths unavailable. |
| Dependency closure | A self-contained shape check accepted a `.deps.json` runtime assembly that was absent from the publish tree. | The regression fails RED, then runtime dependency-closure validation rejects the named missing assembly before manifest acceptance. |
| Atomic source binding | Approval was captured from the live repository before the build lock, and restore/publish still referenced live projects. | The lock covers one-shot staging, a snapshotted strict approval, staged-only locked commands, both smokes, source/runtime manifests, child exit, and protection restoration. Source equality is necessary but no longer sufficient for execution. |
| Same-owner staged writer | The invoking user owns the stage and can reset its DACL, allowing a transient write/compile/restore ABA race while final source still matches. | ACLs, attributes, and retained handles remain fail-closed defense-in-depth, with no immutable-owner claim. A simulated transient source tamper/restoration produces a different runtime manifest and the approval-bound builder rejects it before returning an executable physical build. |
| External build writes | NuGet assets, compiler intermediates, ordinary outputs, and WPF temporary projects could require writes below the staged source. | The protected transaction builds only the runner and watchdog, and `UseArtifactsOutput` redirects all SDK/NuGet/compiler outputs outside source. The real protected locked restore/publish succeeds with no staged `bin`, `obj`, or temporary project writes. |
| Cross-generation runtime equality | Raw staged `obj/project.assets.json` embedded absolute project/output paths, so identical source in new generations could never match PREPARE. | Runtime equality hashes every file in both complete publish trees plus path-independent locks and controls, excludes raw assets JSON, uses audited `PathMap` values, and is now an independent approval value rather than a PREPARE-observed value. |
| Protection cleanup after child failure | Cancellation could unwind and restore source ACLs while a compiler/apphost child remained alive. | Cancellation/failure kills the full process tree and awaits exit before the protection lease unwinds; the injected lifecycle test verifies no active child at release. |
| Mandatory startup smoke | A successful publish was accepted without executing either apphost. | Every publish step carries a direct `--help` smoke with no physical variables and nonexistent `DOTNET_ROOT`, `DOTNET_ROOT_X64`, and `DOTNET_SHARED_STORE`; exit, publish-tree bytes, external artifacts, and temp emptiness are mandatory. |
| Scrubbed NuGet environment | Fresh isolated restore failed because NuGet's machine-wide settings and writable user folders were absent. | Generation-local profile/AppData plus empty generation-local program-files/common-data stand-ins preserve isolation and allow locked restore. |
| Execution child environment | Hostile ambient runtime, profiler, build, test-host, and arbitrary variables remained in the runner `ProcessStartInfo`. | The watchdog clears the child environment, admits only four validated Windows path essentials and the exact authenticated execute/recovery contract, and rejects extra keys, unsafe values, or identity drift before launch. |
| Narrow process-conflict inspection | An actual read-only PREPARE attempt failed before HID access when unrestricted `Win32_Process` enumeration raised `COMException: Unspecified error` while reading an unrelated row's `ProcessId`. | Ten focused tests prove empty/known-conflict behavior, exact native WPF lookup, narrow DawnPro/Python WMI clauses, legacy Python coverage, unrelated inaccessible-row isolation, safe exit races, and precise fail-closed relevant-candidate errors. A software-only call through the production guard also passed on the actual current Windows host with zero known conflicts. |
| FW1.5 raw Active EQ 9 | `SnapshotValidationAcceptsObservedDawnPro2Firmware15RawActiveEqNine` failed 0/1 because the validator universally required `7`. | The exact DAWN PRO2 model, VID/PID, firmware `1.5`, raw value `9` path passes; wrong model, PID, firmware, and other-value regressions remain fail-closed. |
| Exact raw firmware string | The same focused command passed exact `"1.5"` but failed all four rows for `" 1.5"`, `"1.5 "`, `"\t1.5"`, and `"1.5\r"`; `Trim()` produced no restoration problems for any row. | Removing both trims made all 5/5 focused cases pass with ordinal raw-string comparison; the DAWN PRO2 model, VID/PID, and raw Active EQ `9` conditions are otherwise unchanged. |
| Exact identity metadata | `SnapshotValidationRejectsRawActiveEqNineForWrongDeviceModel` failed compilation with `CS0117` because the retained identity had no model field. | Persisted identity now carries model, VID, and PID; exact catalog matching and validator coverage passed. |
| Minimum physical mutation | `ExecuteOrchestratorUsesOneTemporaryPeqWriteThenFullRestorationOnly` failed because its first action was the former Active-EQ toggle. | The observable action sequence is exactly one `IndividualBand` mutation followed by `RestoreOriginalRam`; dead gain/bulk/flash/cycle actions were removed. |
| No Active-EQ write | `PhysicalPrepareExecuteAndRecoveryNeverWriteOrToggleActiveEq` failed because the physical source called `WriteActiveEqAsync`. | PREPARE/EXECUTE/RECOVERY contain no Active-EQ write/toggle call; restoration instead demands the fresh full readback retain original raw `9`. |
| One-mutation transition plan | `TransitionPlanDescribesOnlyOneTemporaryPeqBandMutation` failed because the plan changed both gains and described eight bulk mutations. | Baseline and compatibility bulk fields remain byte-equivalent to original, while only one supported band's 0.25 dB delta differs and Active EQ remains `9`. |
| Recovery without flash/cycle | `RecoveryOrchestratorUsesFullRawRestorationOnly` first rejected the intended single-band state under stale bulk reachability. | Recovery accepts only phase-reachable original/single-band state and schedules exactly `RestoreOriginalRam`; it performs no Active-EQ write, flash save, or physical cycle. |
| Complete-output mismatch diagnostics | `CompleteOutputMismatchReportsEveryPathHashSizeExistenceAndDiagnosticTimestamp` failed against the generic message, which contained no tree path or file evidence. | The same test passes and the guard reports all three synthetic mismatch shapes with both paths, existence, SHA-256, size, and diagnostic UTC timestamp; a separate regression proves timestamp-only differences match. |

## Final exact verification

```text
Release solution build:                 0 warnings, 0 errors
Release watchdog build:                 0 warnings, 0 errors
Dedicated physical runner build:        0 warnings, 0 errors
Default-safe full .NET:                 259 passed, 0 failed, 0 skipped
Fifteenth physical-support focus:       88 passed, 0 failed, 0 skipped
Python:                                 124 passed
Runtime approval manifest:              both hashes/counts remain placeholders; PREPARE blocked
Candidate source/runtime SHA-256:        reported in the remediation handoff, not written as approval
Live source calculator:                  174 inputs (117 sentinels, 57 content)
Two clean staged source proofs:          174 inputs each; equal across distinct stages
Two-generation runtime manifest proof:  532 each (331 runner, 192 watchdog, 9 metadata); equal
Direct physical apphost --help:          exit 0 in both generations under hostile shared-runtime env
Direct watchdog apphost --help:          exit 0 in both generations under hostile shared-runtime env
MOONDROP_* environment variables:       none
New hardware snapshot/result/screenshots: 0
Lingering physical runner/watchdog:      0
git diff --check:                       passed
```

The current default full-suite total contains only the 259 selected software tests. The three physical tests are excluded by `tests-dotnet/default.runsettings`, not executed and not reported as skipped. Both fifteenth-remediation generations used only the candidate audit path, completed natively with exit `0`, and matched on both hashes and every declared count; no approval was synthesized, and the live two-hash approval file remains the placeholder. Each generation's builder directly smoked both exact apphosts with hostile shared-runtime paths before accepting the complete runtime manifest. Candidate hashes are reported in the remediation handoff rather than embedded here. The narrow approval verifier rejects the placeholder before approval matching, as required.

## Runtime-integrity mismatch evidence (2026-08-08)

This was offline-only. Physical work stayed paused: no DAC/HID/API access, PREPARE, EXECUTE, RECOVERY, physical category, physical opt-in, approval fill, EQ action, write, flash, commit, or push occurred.

The read-only comparer reported 339 entries between the recursively enumerated normal framework-dependent `bin` tree and the fresh flat self-contained runner. It reported four changed compiled files between the retained older sealed runner and the fresh runner: `Moondrop.Hardware.dll`, `Moondrop.Hardware.pdb`, `Moondrop.PhysicalTests.dll`, and `Moondrop.PhysicalTests.pdb`. Comparing their retained staged sources reported five changed source files. This establishes unsupported host/output shape for the normal build and source drift for the stale sealed build. It does not support MSTest/VSTest generated content, .NET host nondeterminism, publish nondeterminism, timestamp/copied metadata, generated config, testhost, watchdog, or apphost as a cause. Retained same-source generations matched all 331 runner files.

After the diagnostic fix, focused watchdog tests passed 63/63 and the explicit default-safe suite passed 259/259. Candidate A and candidate B were built in distinct isolated generation roots with the candidate-only command and no `MOONDROP_*` variables. Both complete runtime manifests contained 532 inputs: 331 runner, 192 watchdog, and 9 metadata. The source trees matched 58/58, runner trees 331/331, and watchdog trees 192/192 with zero per-file mismatches. Candidate B completed with exit `0`; candidate A exceeded the attaching shell's five-minute observation timeout while hashing the retained artifact corpus, continued to completion, released its source protection, and then independently recaptured to the identical complete runtime manifest before the A/B comparisons. The live approval remains both fail-closed placeholders with all counts zero.
